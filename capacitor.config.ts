import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'varcel-deployement',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
