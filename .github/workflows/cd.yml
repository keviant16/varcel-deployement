name: CI
on: 
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-lastest
    steps:
      - uses: action/checkout@v2
      - uses: action/setup-node@v1
        with:
          node-version: 12
      - run: npm ci
      - run: npm run build

